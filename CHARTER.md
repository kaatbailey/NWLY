# nwproto — Charter

**This document is never regenerated by an AI.** It is edited by hand, by the
project owner, deliberately. Any session that proposes rewriting it is doing the
exact thing that causes drift.

Every chunk prompt quotes §1 of this file verbatim. That is the mechanism: the
goal is re-stated at the top of every session rather than inherited through a
chain of summaries.

---

## 1. What we are building

**A working private server for the New World client**, reached by understanding
the client's network layer well enough to speak to it — not by modifying the
client.

Three layers, built in order:

1. **Transport layer** — know how the client frames, secures and delivers
network messages: the socket path, the crypto boundary, packet headers,
reliability and reassembly. Verified against a reference build we control
before it is verified against the retail client.
2. **Protocol layer** — know what the messages *mean*: the handshake, the
message-type dispatch table, the replicated-object model, and the wire
encoding of the handful of message types a login-to-in-world session needs.
3. **Server layer** — a server that completes a handshake, stands a character
in the world, and serves enough state that the client renders and moves.

**Why the layering is in this order.** Every attempt to guess message meaning
before the transport is understood produces plausible byte-diffs that cannot be
falsified. The transport tells us where the plaintext, framed messages are; only
then does guessing at their meaning become a test rather than a story.

If a proposed piece of work does not eventually serve completing a handshake,
serving world state, or understanding the messages that do those two things, it
is out of scope.

---

## 2. The reference build is not a side project — it is the primary instrument

We have the Lumberyard fork the client was built from. A GridMate `Carrier` test
(or whatever the fingerprint proves the client uses) built from that fork, run
between two processes we control, with symbols and full source and no runtime
protections, is the independent source against which everything about the retail
client is checked.

It exists for three bounded reasons:

- It gives a **known-good handshake and packet-header layout** to diff the
retail client's opening bytes against — the single fastest way to identify the
protocol.
- It is where the tooling is developed and validated: the crypto hook, the
dispatch-table walker, the reflection reader all get built and proven against a
target that cannot fight back before they are pointed at the retail binary.
- It answers "is this a GridMate fork or a rewrite" definitively, because the
reference *is* stock GridMate and any divergence is measurable.

**When the reference build and the retail client conflict, the retail client is
the truth** — the reference is how we understand it, not what we are trying to
match. A finding from the reference build is a hypothesis about the retail client
until confirmed against the retail client.

---

## 3. Standing non-goals

- **No client modification, no injection into the retail client as the delivery
mechanism.** Injection and hooking are RESEARCH tools for reading plaintext on a
build we control (§2) and, carefully, the retail client for understanding. The
*product* is a server the unmodified client connects to. Decided at project
start.
- **No anti-cheat work of any kind.** Out of scope permanently and completely.
Not "later" — never. The transport research targets the crypto and framing
layer, not any integrity or attestation system. Any finding that only matters
for defeating an integrity check is off-charter and does not get recorded,
pursued, or built on.
- **Nothing that redistributes Amazon's client, its assets, or its binaries.**
Reverse-engineering a protocol for interoperability is the work; shipping their
files is not.
- **No dependency on the client staying on one build for the server's users.**
Research is version-locked by necessity (§4); the server design must assume the
protocol will move under it and be re-derivable, not hand-tuned to offsets.

---

## 4. Method — the rules that actually catch errors

Adapted from hard experience on prior reverse-engineering work. The single
governing rule: **a belief validated only against your own tooling is not
validated.** Read and write can share a wrong assumption and agree.

- **Prove it on the reference build before believing it on retail.** The GridMate
`Carrier` built from our fork is the known-good. A claim about the retail
client's transport that has not been checked against a build we control is a
guess with good production values.
- **Prefer the source to the sample.** Reading GridMate's `Carrier.cpp` describes
the protocol family; capturing one retail session describes one session. When
they disagree, find out why before trusting the capture.
- **Predict the bytes before you dump them.** Name the header layout you expect
from the reference build, then look. A capture that merely "looks structured"
proves nothing; one that matches a prediction, or breaks it cleanly, is a test.
- **Hook above the crypto, never at the socket.** The datagram on the wire is
DTLS ciphertext. `SSL_read` / `SSL_write` (or the mbedTLS / CNG equivalent the
fingerprint proves) hand you plaintext, already framed. A byte-level capture at
`recvfrom` is measuring the wrong layer and will waste the time it takes to
discover that.
- **Ask what would falsify a result.** An entropy test that cannot distinguish
"encrypted" from "compressed" is not a test. Say what each hypothesis predicts
before looking.
- **Change one thing per experiment.** Stand still to isolate the heartbeat; walk
in a straight line to isolate the position message. Two variables at once and
the capture means nothing.
- **A tool's cap is part of the measurement.** A hook that silently drops packets
over a size, a logger that can't keep up with a 60Hz stream — the gap looks
exactly like "the client didn't send it." Know what your instrument can capture
before believing what it did.
- **Version-lock everything, from day one.** Note the exact client build, keep the
installer, signature-scan instead of hardcoding offsets. A patch moves every
address and may move the protocol.

Working style: propose an approach, name the check that would prove it wrong, run
that check on the reference build, then work the retail client.

---

## 5. Document governance

Three kinds of document, three different rules. This is the fix for handoff
drift.

| Document                | Rule                                                                                                                                                                                                     |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CHARTER.md`            | **Never regenerated.** Hand-edited by the owner only.                                                                                                                                                    |
| `STATE.md`              | **Append-only in practice.** A session may add findings and may promote a claim from UNVERIFIED to CONFIRMED with evidence. A session may **not** delete, reorder, or "clean up" — a wrong belief moves to the Corrections table, it does not disappear. |
| `CHUNKS.md`             | The index and the prompts. Amended when a chunk completes or a decision gate resolves.                                                                                                                   |
| Chunk `FINDINGS` blocks | Disposable once folded into `STATE.md`.                                                                                                                                                                  |

**The failure mode this prevents:** a session whose context is entirely one
sub-problem regenerates the handoff doc from what it can see, and the project's
one-line self-description collapses into that sub-problem. No session should ever
be in a position to summarise the goal away. The charter is the part that must
survive; the chunk prompt is disposable. If a future session reads only one
document, it should be this one.

---

## 6. Amendment log

| Date       | Change                                                                                                                            |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------- |
| <!-- fill --> | Charter created. Transport → protocol → server layering fixed. Reference build named as the primary instrument. Anti-cheat ruled out permanently. Client modification ruled out as a delivery mechanism. |
